<h1>Showing All Team</h1>
<%= button_to 'Create Team', new_team_path,
					class: 'btn btn-primary', method: :get%>
<% @teams.each do |team| %>
	<%= render partial: 'team', object: team, locals: { multi: true } %>
<% end %>